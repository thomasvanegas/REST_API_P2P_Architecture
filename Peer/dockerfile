# Imagen base ligera con Python
FROM python:3.12-slim

WORKDIR /app

COPY . /app

RUN pip install requests fastapi uvicorn

# Crear carpeta del volumen compartido
RUN mkdir -p /data

# CMD por defecto: ejecutar bootstrap.py que levanta el peer según config
CMD ["python", "bootstrap.py"]
